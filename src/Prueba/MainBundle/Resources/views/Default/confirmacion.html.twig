{% extends "::base.html.twig" %} {% block body %}
<br>
<div class="container theme-showcase" role="main">
	<div class="jumbotron">
		<p>Los datos consultados son:</p>
		<div class="col-sm-6">
		
	{# 		<table>#}
	{# 			{% for nr,dato in datos %}#}
	{# 			<tr>#}
	{# 				<td><strong>{{ nr }}:</strong></td>#}
	{# 				<td>{{ dato }}</td>#}
	{# 			</tr>#}
	{# 			{% endfor %}#}
	{# 		</table>#}
	
			<table>
				<tr>
					<td><strong>Latitud:</strong></td>
					<td>{{ datos.lat }}</td>
				</tr>
				<tr>
					<td><strong>Longitud:</strong></td>
					<td>{{ datos.lon }}</td>
				</tr>
				<tr>
					<td><strong>País:</strong></td>
					<td>{{ datos.pais }}</td>
				</tr>
				<tr>
					<td><strong>Población:</strong></td>
					<td>{{ datos.poblacion }}</td>
				</tr>
				<tr>
					<td><strong>Región</strong></td>
					<td>{{ datos.region }}</td>
				</tr>
				<tr>
					<td><strong>Estado del tiempo:</strong></td>
					<td>{{ datos.tiempo }}</td>
				</tr>
			</table>
		</div>
		<div class="col-sm-6">
			{#Se añade una imágen de la zona#}
			<img alt="300" border=1 src="https://maps.googleapis.com/maps/api/staticmap?center={{ datos.lat }},{{ datos.lon }}&zoom=14&size=300x300&language=es_ES&sensor=false">
		</div>
		<div class"col-sm-12">
			<div class="fb-like" data-href="{{ app.request.uri }}" data-layout="button_count" data-action="like" data-show-faces="false" data-share="false"></div>
		</div>
	</div>
	</div>
{% endblock %}
